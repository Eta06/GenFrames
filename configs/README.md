# Configurations

Committed experiment configurations live here. A result record must reference
the exact configuration, Git commit, dataset manifest, seed, device, precision,
and checkpoint digest used to produce it.

