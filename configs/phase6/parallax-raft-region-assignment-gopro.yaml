experiment_id: parallax-raft-region-assignment-gopro-001
git_keyword: Parallax

model:
  candidate_fields: 10
  candidate_generation: frozen_fixed_point_iterates
  assignment_classes: 11
  region_stride: 8
  output: straight_through_hard_single_field
  abstain_fallback: pixelwise_highest_cycle_confidence_field
  dense_candidate_blending: false
  rgb_residual: false
  flow_update: false
  trainable_selector_parameters: 86635
  research_oracle_total_parameters: 1076797

data:
  real_weight: 0.85
  synthetic_weight: 0.15
  gopro_within_real_weight: 0.8
  davis_within_real_weight: 0.2
  crop_size: 256

training:
  steps: 1500
  batch_size: 4
  learning_rate: 0.0001
  seed: 7201
  region_assignment_weight: 0.1
  ownership_weight: 0.05
  unsupported_error_threshold: 0.04
  selector_spatial_weight: 0.02

decision:
  representation: retained
  assignment: reject_not_promoted
  blind_ab: blocked_visual_regression
  overwrite: forbidden
